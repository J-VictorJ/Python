# ### Python Tuples
#
# 1. Crie uma tupla chamada `cores` com quatro nomes de cores diferentes. Em seguida, imprima a tupla.
# 
# 2. Acesse o terceiro elemento da tupla `cores` e o último elemento da tupla usando índices. Imprima esses elementos
# separadamente.
#
# 3. Tente atualizar um elemento na tupla `cores`. Você deve receber um erro, pois as tuplas são imutáveis.
# Explique por que isso acontece.
#
# 4. Crie uma tupla chamada `coordenadas` com as coordenadas x, y e z de um ponto em um espaço tridimensional.
# Em seguida, use a técnica de desempacotamento de tuplas para atribuir cada coordenada a variáveis separadas
# e imprima as coordenadas.
#
# 5. Crie uma tupla chamada `nomes` com nomes de pessoas. Use um loop `for` para percorrer a tupla e imprimir cada nome.
#
# 6. Crie uma segunda tupla chamada `nomes2` com mais nomes de pessoas. Use o operador `+` para concatenar as duas
# tuplas em uma terceira tupla chamada `todos_nomes`. Imprima a `todos_nomes`.
#
# 7. Use o método `count()` para contar quantas vezes um elemento específico aparece em uma tupla. Crie uma tupla com
# alguns números repetidos e conte quantas vezes um número específico aparece.
#
# 8. Use o método `index()` para encontrar o índice da primeira ocorrência de um elemento em uma tupla. Crie uma tupla
# com alguns elementos e encontre o índice de um elemento específico.
#
# Esses exercícios devem ajudar você a praticar o uso de tuplas em Python. Se tiver alguma dúvida ou precisar de mais
# esclarecimentos sobre algum exercício, não hesite em perguntar. Boa prática!